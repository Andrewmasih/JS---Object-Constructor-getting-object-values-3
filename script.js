

let tree = {
  species: "oilve tree",
  age: 150,
  location: "greece", 
    olives: {
      amount: 1000,
      color: "Black"
    }
};

console.log (tree. olives. amount);
console.log (tree. olives. color);

/* The 'object constructor' method in the above example is the 3rd way to create an object . If you have an object that has another object as a property,then you can use 'dot notation' twice x2 to get the property of the inner object */